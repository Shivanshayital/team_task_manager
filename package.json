{
  "name": "team-task-manager",
  "version": "1.0.0",
  "description": "Team Task Manager - Fullstack app with React frontend and Express/MongoDB backend",
  "main": "server/server.js",
  "type": "module",
  "scripts": {
    "start": "cd server && npm ci && npm start",
    "dev": "cd server && npm run dev",
    "server:install": "cd server && npm ci",
    "client:install": "cd client && npm ci",
    "client:build": "cd client && npm run build",
    "setup": "npm run server:install && npm run client:install && npm run client:build"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
