{
  "name": "team-task-manager",
  "version": "1.0.0",
  "description": "Team Task Manager - Fullstack app with React frontend and Express/MongoDB backend",
  "main": "server/server.js",
  "type": "module",
  "scripts": {
    "start": "node server/server.js",
    "build": "npm run build:client && npm run build:server",
    "build:client": "npm --prefix client ci --include=dev && npm --prefix client run build",
    "build:server": "npm --prefix server ci --omit=dev",
    "install:client": "npm --prefix client install",
    "install:server": "npm --prefix server install"
  },
  "engines": {
    "node": "22.x",
    "npm": ">=10.0.0"
  }
}
