{
  "name": "team-task-manager",
  "version": "1.0.0",
  "description": "Team Task Manager - Fullstack app with React frontend and Express/MongoDB backend",
  "main": "server/server.js",
  "type": "module",
  "scripts": {
    "start": "sh start.sh",
    "build": "npm run build:client && npm run build:server",
    "build:client": "cd client && npm install --omit=dev && npm run build && cd ..",
    "build:server": "cd server && npm install --omit=dev && cd ..",
    "install": "npm run install:client && npm run install:server",
    "install:client": "cd client && npm install && cd ..",
    "install:server": "cd server && npm install && cd .."
  },
  "engines": {
    "node": ">=14.0.0",
    "npm": ">=6.0.0"
  }
}
